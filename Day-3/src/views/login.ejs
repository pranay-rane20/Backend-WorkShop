<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login | Social Connect</title>
    <!-- Latest Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Remix Icon CDN -->
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.5.0/fonts/remixicon.css" rel="stylesheet" />
</head>

<body class="bg-gradient-to-br from-gray-50 to-gray-100 text-gray-900 h-screen flex items-center justify-center">
    <div class="w-full max-w-md bg-white border-0 rounded-xl px-8 py-10 shadow-lg">
        <!-- Logo -->
        <div class="flex justify-center mb-8">
            <i class="ri-community-line text-6xl text-indigo-600"></i>
        </div>
        <!-- Main Content -->
        <form action="/login" method="POST" class="space-y-6">
            <h1 class="text-2xl font-bold mb-6 text-center text-gray-800">
                Welcome Back
            </h1>
            <div class="space-y-4">
                <input
                    type="text"
                    name="username"
                    placeholder="Username or Email"
                    class="w-full px-4 py-3 border border-gray-200 rounded-lg bg-gray-50 text-gray-900 focus:ring-2 focus:ring-indigo-500 focus:border-transparent focus:outline-none transition duration-200"
                    required
                >
                <input
                    type="password"
                    name="password"
                    placeholder="Password"
                    class="w-full px-4 py-3 border border-gray-200 rounded-lg bg-gray-50 text-gray-900 focus:ring-2 focus:ring-indigo-500 focus:border-transparent focus:outline-none transition duration-200"
                    required
                >
                <div class="flex justify-end">
                    <a href="/forgot-password" class="text-sm text-indigo-600 hover:text-indigo-700">Forgot password?</a>
                </div>
            </div>
            <button
                type="submit"
                class="w-full py-3 bg-indigo-600 text-white font-semibold rounded-lg shadow-md hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2 transition duration-200">
                Sign In
            </button>
        </form>

        <div class="relative my-8">
            <div class="absolute inset-0 flex items-center">
                <div class="w-full border-t border-gray-200"></div>
            </div>
            <div class="relative flex justify-center text-sm">
                <span class="px-4 bg-white text-gray-500">Or continue with</span>
            </div>
        </div>

        <div class="grid grid-cols-2 gap-4 mb-6">
            <button class="flex items-center justify-center px-4 py-2 border border-gray-200 rounded-lg hover:bg-gray-50 transition duration-200">
                <i class="ri-google-fill text-xl mr-2 text-red-500"></i>
                Google
            </button>
            <button class="flex items-center justify-center px-4 py-2 border border-gray-200 rounded-lg hover:bg-gray-50 transition duration-200">
                <i class="ri-github-fill text-xl mr-2"></i>
                GitHub
            </button>
        </div>

        <div class="text-sm text-center text-gray-600">
            <span>Don't have an account? </span>
            <a href="/register" class="text-indigo-600 font-medium hover:text-indigo-700">Create account</a>
        </div>

        <p class="text-xs text-gray-500 mt-8 text-center">
            By continuing, you agree to our 
            <a href="#" class="text-indigo-600 hover:text-indigo-700">Terms of Service</a>,
            <a href="#" class="text-indigo-600 hover:text-indigo-700">Privacy Policy</a>, and
            <a href="#" class="text-indigo-600 hover:text-indigo-700">Cookie Policy</a>.
        </p>
    </div>
</body>

</html>
